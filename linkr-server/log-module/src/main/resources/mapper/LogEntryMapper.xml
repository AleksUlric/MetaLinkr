<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aleks.linkrmix.log.mapper.LogEntryMapper">

    <!-- 统计总日志数 -->
    <select id="countTotalLogs" resultType="java.lang.Long">
        SELECT COUNT(1) FROM log_entry WHERE deleted = 0
    </select>

    <!-- 统计错误日志数 -->
    <select id="countErrorLogs" resultType="java.lang.Long">
        SELECT COUNT(1) FROM log_entry 
        WHERE deleted = 0 AND level IN ('ERROR', 'FATAL')
    </select>

    <!-- 统计警告日志数 -->
    <select id="countWarningLogs" resultType="java.lang.Long">
        SELECT COUNT(1) FROM log_entry 
        WHERE deleted = 0 AND level = 'WARN'
    </select>

    <!-- 统计今日新增日志数 -->
    <select id="countTodayLogs" resultType="java.lang.Long">
        SELECT COUNT(1) FROM log_entry 
        WHERE deleted = 0 
        AND timestamp BETWEEN #{startTime} AND #{endTime}
    </select>

    <!-- 获取最新告警 -->
    <select id="getRecentAlerts" resultType="com.aleks.linkrmix.log.entity.LogEntry">
        SELECT * FROM log_entry 
        WHERE deleted = 0 AND level IN ('ERROR', 'FATAL')
        ORDER BY timestamp DESC 
        LIMIT #{limit}
    </select>

    <!-- 获取日志趋势数据 -->
    <select id="getLogTrend" resultType="java.util.Map">
        SELECT 
            DATE_FORMAT(timestamp, '%H:00') as time,
            COUNT(1) as count
        FROM log_entry 
        WHERE deleted = 0 
        AND timestamp BETWEEN #{startTime} AND #{endTime}
        GROUP BY DATE_FORMAT(timestamp, '%H:00')
        ORDER BY time
    </select>

</mapper>
